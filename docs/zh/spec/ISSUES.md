# 问题

### 分窗与引擎接口

我隐隐感觉这一部分的上下文可以作为全局上下文…… <- 审阅 `Zongzi` 时有感

但没有变基以及结构性冲突的循环的体现。

英文水平降下去了。

### Zongzi.Score

#### TimeSig

查 Wiki 发现简单拍子是 Simple meters 而不是 standard 。

散拍子… 算了，留着占位。

#### Record

这里的 timeline 和 `Zongzi.Timeline` 有什么区别？ <- 审阅 `Zongzi.Score.Record` 时有感

